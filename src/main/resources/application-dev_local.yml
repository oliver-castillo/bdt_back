spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bt_local
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:brandtVeloz123!}
  #    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      #ddl-auto: create-drop
      ddl-auto: none
    #generate-ddl: true
    show-sql: true
