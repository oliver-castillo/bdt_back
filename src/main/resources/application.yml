common-property:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

spring:
  profiles:
    active: dev_local
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher

jwt:
  secret: secret
  expiration: 36000
